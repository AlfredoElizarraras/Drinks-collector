<%= page_name(@drinks_title) %>

<div class="card justify-content-between align-items-center flex-row py-4 px-3">
<span>Left to drink:</span>
<%= drinks_total(@total_drinks) %>
</div>

<%= render partial: '/shared/drink', collection: @drinks %>

<%= link_to  "Add new", new_drink_path %>